namespace LarjeEnum 
{
    public enum KinematicsPointerType { Body, FrontArm, BackArm, FrontLeg, BackLeg }

    public enum PointerMotion 
    {
        BodyCalm,
        ArmCalm,
        LegCalm
    }
}
